global:
  scrape_interval: 1s
  scrape_native_histograms: true

storage:
  tsdb:
    out_of_order_time_window: 10m

scrape_configs:
  - job_name: "prometheus"
    scrape_interval: 5s
    static_configs:
      - targets: ["lgtm:9090"]
